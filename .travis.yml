language: go
go:
    - 1.9
addons:
  apt:
    packages:
      - libc6-dev-armel-cross
      - gcc-arm-linux-gnueabi
before_install:
  - go get github.com/tcnksm/ghr
after_success:
  - make clean
  - GOOS=linux CC=arm-linux-gnueabi-gcc GOARCH=arm GOARM=7 CGO_ENABLED=1 go build -v -o dist/linux_arm_mpdbot/mpdbot ./cmd/mpdbot
  - CGO_ENABLED=1 go build -v -o dist/linux_amd64_mpdbot/mpdbot ./cmd/mpdbot
  - for i in $(find dist -mindepth 1 -maxdepth 1 -type d); do cp {README.md,config.yml} $i; tar -czf "$i".tar.gz -C "$i" "."; done
  - if [ "$TRAVIS_TAG" != "" ]; then ghr --username rendom --token $GITHUB_TOKEN --replace $TRAVIS_TAG dist/; fi
